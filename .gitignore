/dctl
/dctl.exe
/herrscher
/herrscher.exe
*.test

# per-session git worktrees created by `dctl serve`
/.dctl-sessions/

# local secrets, auto-loaded at startup (see .env.example)
/.env
.superpowers/

# local-dev Go workspace (resolves the unmerged gateway branch); never committed
/go.work
/go.work.sum
